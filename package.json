{
  "name": "azure",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest --coverage --coverageDirectory __tests__/coverage --config jest.config.js --runInBand --no-cache --watchAll=false"
  },
  "dependencies": {
    "@azure/functions": "^3.0.0",
    "axios": "^0.27.2",
    "joi": "^17.6.0",
    "joi-oid": "^1.1.2",
    "mongodb": "^4.6.0",
    "redoc-cli": "^0.13.16",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "jest": "^28.1.0",
    "typescript": "^4.0.0"
  }
}
